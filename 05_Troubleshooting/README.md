Issues I faced when tried to deploy the `aws-load-balancer-controller`
    hostname: true
    tolerations on the aws pods/deployment
    my node disk had pressure -  cleanup
    Checking the logs - had to change ClusterIP to NodePort
    ProviderID??

